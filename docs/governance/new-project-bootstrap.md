# New Project Bootstrap

This file mirrors the root bootstrap checklist but keeps it inside the docs
layer so planning and governance references stay repository-local.

Use [../../NEW_PROJECT_BOOTSTRAP.md](../../NEW_PROJECT_BOOTSTRAP.md) as the
main checklist and keep this file aligned when the bootstrap flow changes.

Planning reminder:
- do not stop at publishing a plan document
- activate `NOW` and `NEXT` in canonical queue files in the same turn
