# Modul menu

Na obecnym etapie osobny modul menu (CRUD) nie jest aktywny.

Nawigacja serwisu jest budowana przez bloki i szablony (`templates`) w edytorze blokowym.

Tabela `menus` istnieje historycznie po migracjach, ale nie jest glownym mechanizmem konfiguracji menu w obecnym flow.